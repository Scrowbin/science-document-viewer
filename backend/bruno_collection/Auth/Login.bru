meta {
  name: Login (Obtain JWT)
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/auth/token/
  body: json
  auth: none
}

body:json {
  {
    "username": "researcher",
    "password": "Password123!"
  }
}

vars:post-response {
  token: res.body.access
}
